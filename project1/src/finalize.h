#ifndef FINALIZE_H
#define FINALIZE_H
#include <iostream>
#include <string.h>
#include <cstdlib>
#include <algorithm>

std::string finalize(std::string);
std::string to64(unsigned long , int);

#endif